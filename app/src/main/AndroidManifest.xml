<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moonapps.moonmoon.tabatatimer">

    <application
        android:name=".utils.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:label=""
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".TabataTimer.TabataConfigurationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TabataTimer.TabataRunActivity"
            android:label="@string/runningTimerText"
            android:parentActivityName=".TabataTimer.TabataConfigurationActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TabataTimer.TabataConfigurationActivity" />
        </activity>
    </application>

</manifest>